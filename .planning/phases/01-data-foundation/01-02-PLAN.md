---
phase: 01-data-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/translation.ts
  - lib/theme-mapping.ts
  - lib/domain-dictionary.ts
  - app/api/cron/sync-markets/route.ts
  - app/api/cron/snapshot-prices/route.ts
  - tests/translation.test.ts
  - tests/theme-mapping.test.ts
  - tests/sync-markets.test.ts
autonomous: true
requirements:
  - MKTD-01
  - MKTD-02
must_haves:
  truths:
    - "Cron APIが呼ばれるとPolymarket市場データがDBに保存される"
    - "新規市場検出時にLLMで日本語タイトルと文脈補足が生成される"
    - "ドメイン辞書が翻訳プロンプトに注入され、予測市場用語が正確に訳される"
    - "市場がテーマ（米政治、暗号資産、AI等）に自動分類される"
    - "1時間ごとに価格スナップショットが保存される"
    - "同期の成功・失敗がSyncLogに記録される"
  artifacts:
    - path: "lib/translation.ts"
      provides: "LLM翻訳 + 文脈補足生成"
      exports: ["translateMarket"]
    - path: "lib/theme-mapping.ts"
      provides: "テーマ分類ロジック"
      exports: ["mapTheme"]
    - path: "lib/domain-dictionary.ts"
      provides: "ドメイン特化翻訳辞書"
      exports: ["DOMAIN_DICTIONARY", "buildGlossaryPrompt"]
    - path: "app/api/cron/sync-markets/route.ts"
      provides: "市場データ同期API"
      exports: ["GET"]
    - path: "app/api/cron/snapshot-prices/route.ts"
      provides: "価格スナップショットAPI"
      exports: ["GET"]
  key_links:
    - from: "app/api/cron/sync-markets/route.ts"
      to: "lib/polymarket.ts"
      via: "fetchActiveMarkets() import"
      pattern: "fetchActiveMarkets"
    - from: "app/api/cron/sync-markets/route.ts"
      to: "lib/translation.ts"
      via: "translateMarket() import"
      pattern: "translateMarket"
    - from: "lib/translation.ts"
      to: "lib/domain-dictionary.ts"
      via: "DOMAIN_DICTIONARY import"
      pattern: "DOMAIN_DICTIONARY"
    - from: "app/api/cron/sync-markets/route.ts"
      to: "lib/db.ts"
      via: "prisma.market.upsert"
      pattern: "prisma\\.market\\.upsert"
---

<objective>
Polymarket APIからの市場データ同期パイプラインを構築する。5分間隔のCron Jobで市場データを取得・保存し、新規市場検出時にLLMで日本語タイトル・文脈補足を生成、テーマを自動分類する。

Purpose: MKTD-01（日本語一覧表示）とMKTD-02（文脈補足）のデータ基盤を完成させる
Output: 動作するデータ同期パイプライン、翻訳エンジン、テーマ分類エンジン
</objective>

<execution_context>
@/Users/toikobara/Oddscope/.claude/get-shit-done/workflows/execute-plan.md
@/Users/toikobara/Oddscope/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md

<interfaces>
<!-- Plan 01で作成される型・関数。これらを直接importして使用する。 -->

From lib/types.ts:
```typescript
export type Theme = 'politics' | 'crypto' | 'ai' | 'geopolitics' | 'macro' | 'sports' | 'entertainment' | 'other';
export const THEME_LABELS: Record<Theme, string>;
export interface PolymarketRawMarket {
  id: string; question: string; category: string;
  volume: number; volume24hr: number; price: number;
  active: boolean; end_date_iso?: string;
}
```

From lib/polymarket.ts:
```typescript
export async function fetchActiveMarkets(): Promise<PolymarketRawMarket[]>;
```

From lib/db.ts:
```typescript
export const prisma: PrismaClient;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ドメイン辞書 + LLM翻訳エンジン + テーマ分類</name>
  <files>
    lib/domain-dictionary.ts, lib/translation.ts, lib/theme-mapping.ts,
    tests/translation.test.ts, tests/theme-mapping.test.ts
  </files>
  <read_first>
    .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 3: ドメイン辞書, Pattern 4: テーマ分類, Claude's Discretion セクション)
    .planning/phases/01-data-foundation/01-CONTEXT.md (日本語化 セクション)
    lib/types.ts (Theme型定義、PolymarketRawMarket型)
  </read_first>
  <behavior>
    - テーマ分類: "Will Trump win Pennsylvania?" + category "politics" -> "politics"
    - テーマ分類: "Bitcoin to reach $100k?" + category "crypto" -> "crypto"
    - テーマ分類: "Will GPT-5 be released?" + category "technology" -> "ai"
    - テーマ分類: キーワードなし -> "other"
    - ドメイン辞書: "resolve" -> "決済" が辞書に含まれる
    - ドメイン辞書: "swing state" -> "激戦州" が辞書に含まれる
    - 翻訳: OpenAI APIにドメイン辞書が注入されたプロンプトが送信される
    - 翻訳: レスポンスが { titleJa: string, contextNote: string } 形式で返る
  </behavior>
  <action>
1. lib/domain-dictionary.ts を作成:
   ```typescript
   // ドメイン特化翻訳辞書
   export const DOMAIN_DICTIONARY: Record<string, string> = {
     // 予測市場用語
     'resolve': '決済',
     'resolve YES': 'YESで決済（実現）',
     'resolve NO': 'NOで決済（不実現）',
     'YES/NO': 'YES(実現) / NO(不実現)',
     'whale': '大口投資家',
     'liquidity': '流動性',
     'odds': 'オッズ（確率）',

     // 米政治用語
     'win [state]': '[州]を制する',
     'electoral votes': '選挙人投票',
     'swing state': '激戦州',
     'primary': '予備選挙',
     'nomination': '指名',
     'impeachment': '弾劾',
     'executive order': '大統領令',
     'debt ceiling': '債務上限',
     'government shutdown': '政府閉鎖',
     'midterm': '中間選挙',
     'Speaker of the House': '下院議長',
     'Senate confirmation': '上院承認',
     'filibuster': '議事妨害（フィリバスター）',
     'gerrymandering': 'ゲリマンダリング（選挙区改変）',

     // 暗号資産用語
     'ATH': '史上最高値（All-Time High）',
     'halving': '半減期',
     'ETF': 'ETF（上場投資信託）',
     'spot ETF': '現物ETF',
     'DeFi': '分散型金融（DeFi）',
     'stablecoin': 'ステーブルコイン',

     // AI用語
     'AGI': '汎用人工知能（AGI）',
     'frontier model': 'フロンティアモデル',
     'AI safety': 'AI安全性',

     // 地政学用語
     'sanctions': '制裁',
     'ceasefire': '停戦',
     'annexation': '併合',

     // マクロ経済用語
     'Fed': '米連邦準備制度（FRB）',
     'rate cut': '利下げ',
     'rate hike': '利上げ',
     'CPI': '消費者物価指数（CPI）',
     'recession': '景気後退',
     'soft landing': '軟着陸',
   };

   export function buildGlossaryPrompt(): string {
     return Object.entries(DOMAIN_DICTIONARY)
       .map(([en, ja]) => `- ${en}: ${ja}`)
       .join('\n');
   }
   ```

2. lib/translation.ts を作成:
   ```typescript
   import OpenAI from 'openai';
   import { buildGlossaryPrompt } from './domain-dictionary';
   import type { PolymarketRawMarket } from './types';

   export async function translateMarket(
     market: Pick<PolymarketRawMarket, 'question' | 'category'>
   ): Promise<{ titleJa: string; contextNote: string }> {
     const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });
     const glossary = buildGlossaryPrompt();

     const prompt = `あなたは予測市場の専門翻訳者です。以下の市場タイトルを日本語に翻訳してください。

**市場タイトル:** ${market.question}
**カテゴリ:** ${market.category}

**参照用語集:**
${glossary}

**指示:**
1. タイトルを日本語に翻訳（30文字以内、簡潔に）
2. 日本人が「それ何？」とならないよう、1行（50文字以内）で文脈補足を追加
3. 正確さを優先（自然さより意味の正しさ）
4. 固有名詞は原文のままでもOK（人名等）

**出力形式（JSON）:**
{
  "titleJa": "日本語タイトル",
  "contextNote": "文脈補足（固有名詞・背景情報の簡潔な説明）"
}`;

     const completion = await openai.chat.completions.create({
       model: 'gpt-4o-mini',
       messages: [{ role: 'user', content: prompt }],
       response_format: { type: 'json_object' },
       max_tokens: 200,
       temperature: 0.3,
     });

     const content = completion.choices[0].message.content;
     if (!content) throw new Error('Empty LLM response');

     const result = JSON.parse(content);
     return {
       titleJa: result.titleJa ?? '',
       contextNote: result.contextNote ?? '',
     };
   }
   ```

3. lib/theme-mapping.ts を作成:
   ```typescript
   import type { Theme } from './types';

   const THEME_KEYWORDS: Record<Theme, string[]> = {
     politics: ['election', 'trump', 'biden', 'harris', 'senate', 'congress', 'electoral', 'governor', 'president', 'democrat', 'republican', 'gop', 'dnc', 'rnc', 'vote', 'poll', 'ballot', 'impeach'],
     crypto: ['bitcoin', 'ethereum', 'btc', 'eth', 'crypto', 'blockchain', 'defi', 'nft', 'solana', 'stablecoin', 'binance', 'coinbase', 'etf'],
     ai: ['ai ', 'artificial intelligence', 'chatgpt', 'openai', 'llm', 'gpt', 'claude', 'gemini', 'agi', 'machine learning', 'deepmind', 'anthropic'],
     geopolitics: ['ukraine', 'russia', 'china', 'taiwan', 'iran', 'israel', 'gaza', 'nato', 'war', 'invasion', 'sanctions', 'ceasefire', 'north korea'],
     macro: ['fed', 'interest rate', 'gdp', 'inflation', 'recession', 'cpi', 'unemployment', 'treasury', 'bond', 'stock market', 's&p', 'nasdaq', 'dow'],
     sports: ['nba', 'nfl', 'mlb', 'nhl', 'world cup', 'olympics', 'premier league', 'super bowl', 'championship', 'mvp', 'ufc'],
     entertainment: ['movie', 'oscar', 'emmy', 'grammy', 'box office', 'netflix', 'spotify', 'album', 'award'],
     other: [],
   };

   export function mapTheme(category: string, question: string): Theme {
     const text = `${category} ${question}`.toLowerCase();

     for (const [theme, keywords] of Object.entries(THEME_KEYWORDS)) {
       if (theme === 'other') continue;
       if (keywords.some((kw) => text.includes(kw.toLowerCase()))) {
         return theme as Theme;
       }
     }

     return 'other';
   }
   ```

4. tests/theme-mapping.test.ts を作成（RED -> GREEN）:
   - mapTheme('politics', 'Will Trump win Pennsylvania?') === 'politics'
   - mapTheme('crypto', 'Bitcoin to reach $100k?') === 'crypto'
   - mapTheme('technology', 'Will GPT-5 be released?') === 'ai'
   - mapTheme('misc', 'Some random market') === 'other'
   - mapTheme('', 'Fed rate cut in March?') === 'macro'
   - mapTheme('', 'Ukraine ceasefire by June?') === 'geopolitics'

5. tests/translation.test.ts を作成（OpenAI APIをモック）:
   - translateMarket が { titleJa, contextNote } を返すこと
   - プロンプトにドメイン辞書が含まれること
   - 空レスポンス時にエラーがthrowされること
  </action>
  <verify>
    <automated>npx vitest run tests/theme-mapping.test.ts tests/translation.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/domain-dictionary.ts contains "export const DOMAIN_DICTIONARY"
    - lib/domain-dictionary.ts contains "'resolve': '決済'"
    - lib/domain-dictionary.ts contains "'swing state': '激戦州'"
    - lib/domain-dictionary.ts contains "export function buildGlossaryPrompt"
    - lib/translation.ts contains "export async function translateMarket"
    - lib/translation.ts contains "gpt-4o-mini"
    - lib/translation.ts contains "response_format: { type: 'json_object' }"
    - lib/translation.ts contains "buildGlossaryPrompt"
    - lib/theme-mapping.ts contains "export function mapTheme"
    - lib/theme-mapping.ts contains "THEME_KEYWORDS"
    - tests/theme-mapping.test.ts contains "politics"
    - tests/translation.test.ts contains "translateMarket"
    - npx vitest run tests/theme-mapping.test.ts exits with code 0
    - npx vitest run tests/translation.test.ts exits with code 0
  </acceptance_criteria>
  <done>ドメイン辞書・翻訳・テーマ分類の3モジュールが実装され、全テストがパス</done>
</task>

<task type="auto">
  <name>Task 2: 市場データ同期Cron Job + 価格スナップショット</name>
  <files>
    app/api/cron/sync-markets/route.ts,
    app/api/cron/snapshot-prices/route.ts,
    tests/sync-markets.test.ts
  </files>
  <read_first>
    .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 2: Cron Job, Pitfall 1: API認証遅延, Pitfall 3: 同期失敗 セクション)
    lib/polymarket.ts (fetchActiveMarkets関数のシグネチャ)
    lib/translation.ts (translateMarket関数のシグネチャ)
    lib/theme-mapping.ts (mapTheme関数のシグネチャ)
    lib/db.ts (prisma clientのimportパス)
    prisma/schema.prisma (Market, PriceSnapshot, SyncLogモデル構造)
  </read_first>
  <action>
1. app/api/cron/sync-markets/route.ts を作成:
   ```typescript
   import { NextResponse } from 'next/server';
   import { prisma } from '@/lib/db';
   import { fetchActiveMarkets } from '@/lib/polymarket';
   import { translateMarket } from '@/lib/translation';
   import { mapTheme } from '@/lib/theme-mapping';

   export const dynamic = 'force-dynamic';

   export async function GET(request: Request) {
     // CRON_SECRET検証
     const authHeader = request.headers.get('authorization');
     if (authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
       return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
     }

     const syncLog = await prisma.syncLog.create({
       data: { status: 'running' },
     });

     try {
       const rawMarkets = await fetchActiveMarkets();
       let newCount = 0;

       for (const raw of rawMarkets) {
         const existing = await prisma.market.findUnique({
           where: { polymarketId: raw.id },
         });

         let titleJa = existing?.titleJa ?? null;
         let contextNote = existing?.contextNote ?? null;
         let theme = existing?.theme ?? null;

         // 新規市場: 翻訳 + テーマ分類
         if (!existing) {
           newCount++;
           theme = mapTheme(raw.category, raw.question);

           try {
             const translation = await translateMarket({
               question: raw.question,
               category: raw.category,
             });
             titleJa = translation.titleJa;
             contextNote = translation.contextNote;
           } catch (err) {
             console.error(`Translation failed for market ${raw.id}:`, err);
             // 翻訳失敗時はnullのまま保存（後でバッチ回収）
           }
         }

         // 24h変動率の計算（既存データがある場合）
         const change24h = existing
           ? raw.price - existing.currentPrice
           : 0;

         await prisma.market.upsert({
           where: { polymarketId: raw.id },
           update: {
             currentPrice: raw.price,
             volume24h: raw.volume24hr,
             change24h,
             lastSyncedAt: new Date(),
           },
           create: {
             polymarketId: raw.id,
             question: raw.question,
             category: raw.category,
             titleJa,
             contextNote,
             theme,
             currentPrice: raw.price,
             volume24h: raw.volume24hr,
             change24h: 0,
             lastSyncedAt: new Date(),
           },
         });
       }

       // 同期ログ更新
       await prisma.syncLog.update({
         where: { id: syncLog.id },
         data: {
           completedAt: new Date(),
           marketsCount: rawMarkets.length,
           newMarkets: newCount,
           status: 'success',
         },
       });

       return NextResponse.json({
         success: true,
         synced: rawMarkets.length,
         newMarkets: newCount,
       });
     } catch (error) {
       const errorMessage = error instanceof Error ? error.message : 'Unknown error';
       console.error('Market sync error:', error);

       await prisma.syncLog.update({
         where: { id: syncLog.id },
         data: {
           completedAt: new Date(),
           error: errorMessage,
           status: 'error',
         },
       });

       return NextResponse.json(
         { error: 'Sync failed', details: errorMessage },
         { status: 500 }
       );
     }
   }
   ```

2. app/api/cron/snapshot-prices/route.ts を作成（1時間ごと実行）:
   ```typescript
   import { NextResponse } from 'next/server';
   import { prisma } from '@/lib/db';

   export const dynamic = 'force-dynamic';

   export async function GET(request: Request) {
     const authHeader = request.headers.get('authorization');
     if (authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
       return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
     }

     try {
       const activeMarkets = await prisma.market.findMany({
         where: { isActive: true },
         select: { id: true, currentPrice: true, volume24h: true },
       });

       await prisma.priceSnapshot.createMany({
         data: activeMarkets.map((m) => ({
           marketId: m.id,
           price: m.currentPrice,
           volume: m.volume24h,
         })),
       });

       // 30日以上前のスナップショットを削除
       const thirtyDaysAgo = new Date();
       thirtyDaysAgo.setDate(thirtyDaysAgo.getDate() - 30);

       const deleted = await prisma.priceSnapshot.deleteMany({
         where: { timestamp: { lt: thirtyDaysAgo } },
       });

       return NextResponse.json({
         success: true,
         snapshots: activeMarkets.length,
         deletedOld: deleted.count,
       });
     } catch (error) {
       const errorMessage = error instanceof Error ? error.message : 'Unknown error';
       console.error('Snapshot error:', error);
       return NextResponse.json(
         { error: 'Snapshot failed', details: errorMessage },
         { status: 500 }
       );
     }
   }
   ```

3. tests/sync-markets.test.ts を作成:
   - CRON_SECRET未設定時に401が返ること
   - 正常同期時にSyncLogが"success"で記録されること
   - 新規市場でtranslateMarketとmapThemeが呼ばれること
   - 翻訳失敗時もデータが保存されること（titleJa=null）
   - エラー時にSyncLogが"error"で記録されること
  </action>
  <verify>
    <automated>npx vitest run tests/sync-markets.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/api/cron/sync-markets/route.ts contains "export async function GET"
    - app/api/cron/sync-markets/route.ts contains "Bearer ${process.env.CRON_SECRET}"
    - app/api/cron/sync-markets/route.ts contains "fetchActiveMarkets"
    - app/api/cron/sync-markets/route.ts contains "translateMarket"
    - app/api/cron/sync-markets/route.ts contains "mapTheme"
    - app/api/cron/sync-markets/route.ts contains "prisma.market.upsert"
    - app/api/cron/sync-markets/route.ts contains "prisma.syncLog"
    - app/api/cron/snapshot-prices/route.ts contains "export async function GET"
    - app/api/cron/snapshot-prices/route.ts contains "prisma.priceSnapshot.createMany"
    - app/api/cron/snapshot-prices/route.ts contains "deleteMany"
    - tests/sync-markets.test.ts contains "401"
    - tests/sync-markets.test.ts contains "success"
    - npx vitest run tests/sync-markets.test.ts exits with code 0
  </acceptance_criteria>
  <done>市場同期Cron Jobが5分間隔で動作し、新規市場の翻訳・テーマ分類を行い、同期ログを記録する。価格スナップショットが1時間ごとに保存され、30日超の古いデータが削除される。</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` で全テスト（translation, theme-mapping, sync-markets）がパス
- `npm run build` がエラーなしで完了
- app/api/cron/sync-markets/route.ts が fetchActiveMarkets -> translateMarket -> mapTheme -> prisma.market.upsert の一連のフローを実装
</verification>

<success_criteria>
- ドメイン辞書に予測市場・米政治・暗号資産・AI・地政学・マクロ経済の用語が含まれる
- LLM翻訳がgpt-4o-miniを使用し、ドメイン辞書をプロンプトに注入する
- テーマ分類がキーワードマッチングで8テーマに分類する
- 市場同期CronがCRON_SECRET認証付きで実装されている
- 翻訳失敗時もデータ保存が継続する（フォールバック設計）
- SyncLogで同期の成功・失敗が追跡可能
- 価格スナップショットが1時間粒度で保存され、30日超が自動削除される
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-02-SUMMARY.md`
</output>
