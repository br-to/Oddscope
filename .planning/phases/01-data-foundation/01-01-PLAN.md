---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - app/layout.tsx
  - app/(dashboard)/layout.tsx
  - app/(dashboard)/page.tsx
  - prisma/schema.prisma
  - lib/db.ts
  - lib/types.ts
  - lib/polymarket.ts
  - .env.example
  - vercel.json
  - vitest.config.ts
  - tests/setup.ts
  - components.json
autonomous: true
requirements:
  - MKTD-01
must_haves:
  truths:
    - "Next.jsプロジェクトがnpm run devで起動し、ブラウザでページが表示される"
    - "Prismaスキーマが定義され、マイグレーションが実行可能"
    - "Polymarket APIクライアントが型安全に市場データを取得できる"
    - "Vitestが設定され、テストコマンドが実行可能"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Market, PriceSnapshot モデル定義"
      contains: "model Market"
    - path: "lib/polymarket.ts"
      provides: "Polymarket APIクライアント"
      exports: ["fetchActiveMarkets"]
    - path: "lib/types.ts"
      provides: "共通型定義"
      exports: ["Market", "Theme", "FilterParams"]
    - path: "lib/db.ts"
      provides: "Prisma client singleton"
      exports: ["prisma"]
  key_links:
    - from: "lib/polymarket.ts"
      to: "@polymarket/clob-client"
      via: "ClobClient import"
      pattern: "ClobClient"
    - from: "lib/db.ts"
      to: "prisma/schema.prisma"
      via: "PrismaClient generation"
      pattern: "PrismaClient"
user_setup:
  - service: neon
    why: "PostgreSQLデータベース"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> Project -> Connection Details -> Connection string"
  - service: polymarket
    why: "市場データ取得"
    env_vars:
      - name: POLYMARKET_API_KEY
        source: "Polymarket CLOB API keyを取得（https://docs.polymarket.com）"
  - service: openai
    why: "LLM翻訳（Plan 02で使用、ここでenv var定義のみ）"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API Keys"
---

<objective>
Phase 1の基盤となるプロジェクトスキャフォールディングを完了する。Next.js 16.2 + Prisma 7.5 + Neon PostgreSQLのセットアップ、データベーススキーマ定義、Polymarket APIクライアント作成、共通型定義、テスト基盤構築を行う。

Purpose: 後続のPlan 02（データパイプライン）とPlan 03（UI）が並列実行できる土台を作る
Output: 動作するNext.jsプロジェクト、DBスキーマ、APIクライアント、型定義、テスト基盤
</objective>

<execution_context>
@/Users/toikobara/Oddscope/.claude/get-shit-done/workflows/execute-plan.md
@/Users/toikobara/Oddscope/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.jsプロジェクト初期化 + DB スキーマ定義</name>
  <files>
    package.json, tsconfig.json, next.config.ts, tailwind.config.ts,
    app/layout.tsx, app/(dashboard)/layout.tsx, app/(dashboard)/page.tsx,
    prisma/schema.prisma, lib/db.ts, .env.example, vercel.json, components.json
  </files>
  <read_first>
    .planning/phases/01-data-foundation/01-RESEARCH.md (Standard Stack, Architecture Patterns, Code Examples セクション)
    .planning/phases/01-data-foundation/01-UI-SPEC.md (Design System, Component Inventory セクション)
  </read_first>
  <action>
1. Next.js 16.2プロジェクトを作成:
   ```bash
   npx create-next-app@16.2.0 . --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*"
   ```
   もし既にディレクトリが空でない場合は手動でpackage.jsonから構築する。

2. 追加パッケージをインストール:
   ```bash
   # Core
   npm install @neondatabase/serverless @prisma/client@7.5.0
   npm install -D prisma@7.5.0

   # Polymarket
   npm install @polymarket/clob-client@5.8.0

   # LLM
   npm install openai

   # UI & Forms
   npm install react-hook-form @hookform/resolvers zod swr date-fns

   # Testing
   npm install -D vitest @vitejs/plugin-react @testing-library/react @testing-library/jest-dom jsdom
   ```

3. shadcn/ui初期化 + コンポーネント追加:
   ```bash
   npx shadcn init
   npx shadcn add table badge select input button skeleton
   ```
   Icon library: lucide-react（shadcn/uiの依存で自動インストール）

4. prisma/schema.prisma を作成（以下の内容を正確に記述）:
   ```prisma
   generator client {
     provider = "prisma-client-js"
   }

   datasource db {
     provider = "postgresql"
     url      = env("DATABASE_URL")
   }

   model Market {
     id              String   @id @default(cuid())
     polymarketId    String   @unique
     question        String
     category        String
     titleJa         String?
     contextNote     String?
     theme           String?
     currentPrice    Float
     volume24h       Float
     change24h       Float
     isActive        Boolean  @default(true)
     endDate         DateTime?
     lastSyncedAt    DateTime @default(now())
     createdAt       DateTime @default(now())
     updatedAt       DateTime @updatedAt

     priceSnapshots  PriceSnapshot[]

     @@index([theme])
     @@index([volume24h])
     @@index([change24h])
     @@index([isActive])
     @@index([isActive, volume24h])
   }

   model PriceSnapshot {
     id        String   @id @default(cuid())
     marketId  String
     market    Market   @relation(fields: [marketId], references: [id], onDelete: Cascade)
     price     Float
     volume    Float
     timestamp DateTime @default(now())

     @@index([marketId, timestamp])
   }

   model SyncLog {
     id          String   @id @default(cuid())
     startedAt   DateTime @default(now())
     completedAt DateTime?
     marketsCount Int?
     newMarkets   Int?
     error       String?
     status      String   @default("running")
   }
   ```

5. lib/db.ts を作成（Prisma client singleton）:
   ```typescript
   import { PrismaClient } from '@prisma/client';

   const globalForPrisma = globalThis as unknown as { prisma: PrismaClient };

   export const prisma = globalForPrisma.prisma || new PrismaClient();

   if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma;
   ```

6. .env.example を作成:
   ```
   DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
   POLYMARKET_API_KEY=your_polymarket_api_key
   OPENAI_API_KEY=your_openai_api_key
   CRON_SECRET=your_cron_secret_here
   ```

7. vercel.json を作成:
   ```json
   {
     "crons": [
       {
         "path": "/api/cron/sync-markets",
         "schedule": "*/5 * * * *"
       },
       {
         "path": "/api/cron/snapshot-prices",
         "schedule": "0 * * * *"
       }
     ]
   }
   ```

8. app/layout.tsx: ルートレイアウトにInter fontとメタデータを設定。lang="ja"を指定。
9. app/(dashboard)/layout.tsx: ダッシュボード共通レイアウト（container mx-auto等）。
10. app/(dashboard)/page.tsx: 仮のServer Component（"予測市場一覧" heading + "準備中" placeholder）。

11. `npx prisma generate` を実行してPrisma Clientを生成。
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "@polymarket/clob-client": "5.8.0"
    - package.json contains "@prisma/client": "7.5.0"
    - package.json contains "next": version starting with "16"
    - prisma/schema.prisma contains "model Market"
    - prisma/schema.prisma contains "model PriceSnapshot"
    - prisma/schema.prisma contains "model SyncLog"
    - prisma/schema.prisma contains "@@index([isActive, volume24h])"
    - lib/db.ts contains "export const prisma"
    - lib/db.ts contains "PrismaClient"
    - .env.example contains "DATABASE_URL="
    - .env.example contains "POLYMARKET_API_KEY="
    - .env.example contains "OPENAI_API_KEY="
    - .env.example contains "CRON_SECRET="
    - vercel.json contains "*/5 * * * *"
    - app/layout.tsx contains 'lang="ja"'
    - app/(dashboard)/page.tsx exists
    - components.json exists (shadcn/ui initialized)
    - npx prisma validate exits with code 0
  </acceptance_criteria>
  <done>Next.jsプロジェクトがビルド成功し、Prismaスキーマが検証パスし、shadcn/uiが初期化されている</done>
</task>

<task type="auto">
  <name>Task 2: 共通型定義 + Polymarket APIクライアント + テスト基盤</name>
  <files>
    lib/types.ts, lib/polymarket.ts, vitest.config.ts, tests/setup.ts,
    tests/polymarket.test.ts
  </files>
  <read_first>
    .planning/phases/01-data-foundation/01-RESEARCH.md (Code Examples, Pattern 4: テーマ分類 セクション)
    lib/db.ts (Task 1で作成、importパスの確認)
    prisma/schema.prisma (Task 1で作成、モデル構造の確認)
  </read_first>
  <action>
1. lib/types.ts を作成 — フロントエンド・バックエンド共通の型定義:
   ```typescript
   // テーマ型定義
   export type Theme =
     | 'politics'
     | 'crypto'
     | 'ai'
     | 'geopolitics'
     | 'macro'
     | 'sports'
     | 'entertainment'
     | 'other';

   export const THEME_LABELS: Record<Theme, string> = {
     politics: '米政治',
     crypto: '暗号資産',
     ai: 'AI',
     geopolitics: '地政学',
     macro: 'マクロ経済',
     sports: 'スポーツ',
     entertainment: 'エンタメ',
     other: 'その他',
   };

   // フィルタパラメータ
   export interface FilterParams {
     theme?: Theme;
     minVolume?: number;
     sortBy?: 'volume' | 'change' | 'time';
     showSpikeOnly?: boolean;
   }

   // 市場表示用の型（UIコンポーネントのprops）
   export interface MarketDisplay {
     id: string;
     titleJa: string | null;
     question: string;
     contextNote: string | null;
     theme: string | null;
     currentPrice: number;
     change24h: number;
     volume24h: number;
     isSpike: boolean;
     updatedAt: Date;
   }

   // Polymarket APIレスポンスの型
   export interface PolymarketRawMarket {
     id: string;
     question: string;
     category: string;
     volume: number;
     volume24hr: number;
     price: number;
     active: boolean;
     end_date_iso?: string;
   }
   ```

2. lib/polymarket.ts を作成 — APIクライアント:
   ```typescript
   import { ClobClient } from '@polymarket/clob-client';
   import type { PolymarketRawMarket } from './types';

   const POLYMARKET_HOST = 'https://clob.polymarket.com';
   const MIN_VOLUME_USD = 1000; // $1K minimum

   function createClient(): ClobClient {
     return new ClobClient({
       host: POLYMARKET_HOST,
       key: process.env.POLYMARKET_API_KEY,
     });
   }

   export async function fetchActiveMarkets(): Promise<PolymarketRawMarket[]> {
     const client = createClient();

     const markets = await client.getMarkets({ active: true });

     // 出来高$1K以上のアクティブ市場をフィルタ
     return markets
       .filter((m: any) => (m.volumeNum ?? m.volume ?? 0) >= MIN_VOLUME_USD)
       .map((m: any) => ({
         id: m.condition_id ?? m.id,
         question: m.question ?? '',
         category: m.category ?? '',
         volume: m.volumeNum ?? m.volume ?? 0,
         volume24hr: m.volume24hr ?? 0,
         price: m.price ?? m.bestAsk ?? 0,
         active: m.active ?? true,
         end_date_iso: m.end_date_iso,
       }));
   }

   export { MIN_VOLUME_USD, POLYMARKET_HOST };
   ```
   注意: @polymarket/clob-clientの型は不安定なのでany経由でマッピングする。

3. vitest.config.ts を作成:
   ```typescript
   import { defineConfig } from 'vitest/config';
   import react from '@vitejs/plugin-react';
   import path from 'path';

   export default defineConfig({
     plugins: [react()],
     test: {
       environment: 'jsdom',
       setupFiles: ['./tests/setup.ts'],
       globals: true,
       coverage: {
         provider: 'v8',
         reporter: ['text', 'lcov'],
       },
     },
     resolve: {
       alias: {
         '@': path.resolve(__dirname, '.'),
       },
     },
   });
   ```

4. tests/setup.ts を作成:
   ```typescript
   import '@testing-library/jest-dom/vitest';

   // Prisma mock
   vi.mock('@/lib/db', () => ({
     prisma: {
       market: {
         findMany: vi.fn(),
         findUnique: vi.fn(),
         upsert: vi.fn(),
         count: vi.fn(),
       },
       priceSnapshot: {
         create: vi.fn(),
         createMany: vi.fn(),
       },
       syncLog: {
         create: vi.fn(),
         update: vi.fn(),
       },
     },
   }));
   ```

5. tests/polymarket.test.ts を作成:
   ```typescript
   import { describe, it, expect, vi } from 'vitest';
   import { MIN_VOLUME_USD, POLYMARKET_HOST } from '@/lib/polymarket';

   describe('Polymarket client', () => {
     it('defines correct API host', () => {
       expect(POLYMARKET_HOST).toBe('https://clob.polymarket.com');
     });

     it('defines minimum volume threshold at $1000', () => {
       expect(MIN_VOLUME_USD).toBe(1000);
     });
   });
   ```

6. package.json に test script を追加: `"test": "vitest run --reporter=verbose"`
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/types.ts contains "export type Theme ="
    - lib/types.ts contains "export const THEME_LABELS"
    - lib/types.ts contains "export interface FilterParams"
    - lib/types.ts contains "export interface MarketDisplay"
    - lib/types.ts contains "export interface PolymarketRawMarket"
    - lib/polymarket.ts contains "export async function fetchActiveMarkets"
    - lib/polymarket.ts contains "ClobClient"
    - lib/polymarket.ts contains "MIN_VOLUME_USD = 1000"
    - vitest.config.ts contains "environment: 'jsdom'"
    - vitest.config.ts contains "setupFiles"
    - tests/setup.ts contains "vi.mock('@/lib/db'"
    - tests/polymarket.test.ts contains "describe('Polymarket client'"
    - npx vitest run exits with code 0
  </acceptance_criteria>
  <done>共通型が定義され、PolymarketクライアントがexportされVitestテストがパスする</done>
</task>

</tasks>

<verification>
- `npm run build` がエラーなしで完了
- `npx prisma validate` がパス
- `npx vitest run --reporter=verbose` が全テストパス
- `npm run dev` でlocalhost:3000にアクセスするとページが表示される
</verification>

<success_criteria>
- Next.js 16.2プロジェクトがビルド・起動可能
- Prismaスキーマ（Market, PriceSnapshot, SyncLog）が定義・検証済み
- Polymarket APIクライアントが型安全に定義
- 共通型（Theme, FilterParams, MarketDisplay）がエクスポート
- shadcn/uiが初期化され、必要コンポーネントがインストール済み
- Vitestが設定され、テストが実行可能
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
