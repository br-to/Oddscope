---
phase: 01-data-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components/market-table.tsx
  - components/market-filters.tsx
  - components/market-row.tsx
  - components/empty-state.tsx
  - app/(dashboard)/page.tsx
  - app/(dashboard)/loading.tsx
  - app/(dashboard)/error.tsx
  - lib/queries.ts
  - tests/market-filters.test.ts
autonomous: false
requirements:
  - MKTD-01
  - MKTD-04
must_haves:
  truths:
    - "ユーザーはPolymarket市場の一覧を日本語で閲覧できる"
    - "市場タイトルと文脈補足が日本語で表示される"
    - "テーマタグ、現在確率、24h変動、出来高が各行に表示される"
    - "テーマ・出来高・変動率でフィルタリングできる"
    - "出来高順・変動率順・更新順でソートできる"
    - "フィルタ状態がURLに反映され、リロードしても保持される"
    - "ローディング・エラー・空状態がそれぞれ適切に表示される"
    - "モバイルでもテーブルが崩れず閲覧可能"
  artifacts:
    - path: "components/market-table.tsx"
      provides: "市場一覧テーブルのClient Component"
      min_lines: 50
    - path: "components/market-filters.tsx"
      provides: "フィルタUIのClient Component"
      exports: ["MarketFilters"]
    - path: "app/(dashboard)/page.tsx"
      provides: "ダッシュボードServer Component"
      contains: "prisma.market.findMany"
    - path: "lib/queries.ts"
      provides: "DBクエリ関数"
      exports: ["getFilteredMarkets"]
  key_links:
    - from: "app/(dashboard)/page.tsx"
      to: "lib/queries.ts"
      via: "getFilteredMarkets() import"
      pattern: "getFilteredMarkets"
    - from: "app/(dashboard)/page.tsx"
      to: "components/market-table.tsx"
      via: "MarketTable component render"
      pattern: "<MarketTable"
    - from: "components/market-filters.tsx"
      to: "URL searchParams"
      via: "router.push with URLSearchParams"
      pattern: "router\\.push"
---

<objective>
市場一覧のフロントエンドUIを構築する。Server Componentでデータ取得、Client Componentでフィルタ・テーブル表示を実装し、レスポンシブ対応・ローディング・エラー状態を含む完全なダッシュボード体験を提供する。

Purpose: MKTD-01（日本語一覧表示）とMKTD-04（フィルタリング・ソート）のUI実装を完了する
Output: 動作するダッシュボードUI（テーブル + フィルタ + レスポンシブ + 状態表示）
</objective>

<execution_context>
@/Users/toikobara/Oddscope/.claude/get-shit-done/workflows/execute-plan.md
@/Users/toikobara/Oddscope/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-UI-SPEC.md

<interfaces>
<!-- Plan 01で作成される型。これらを直接importして使用する。 -->

From lib/types.ts:
```typescript
export type Theme = 'politics' | 'crypto' | 'ai' | 'geopolitics' | 'macro' | 'sports' | 'entertainment' | 'other';
export const THEME_LABELS: Record<Theme, string> = {
  politics: '米政治', crypto: '暗号資産', ai: 'AI', geopolitics: '地政学',
  macro: 'マクロ経済', sports: 'スポーツ', entertainment: 'エンタメ', other: 'その他',
};
export interface FilterParams {
  theme?: Theme; minVolume?: number; sortBy?: 'volume' | 'change' | 'time'; showSpikeOnly?: boolean;
}
export interface MarketDisplay {
  id: string; titleJa: string | null; question: string; contextNote: string | null;
  theme: string | null; currentPrice: number; change24h: number; volume24h: number;
  isSpike: boolean; updatedAt: Date;
}
```

From lib/db.ts:
```typescript
export const prisma: PrismaClient;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DBクエリ関数 + Server Component + Loading/Error</name>
  <files>
    lib/queries.ts, app/(dashboard)/page.tsx,
    app/(dashboard)/loading.tsx, app/(dashboard)/error.tsx
  </files>
  <read_first>
    .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 1: Server Components, Code Examples セクション)
    .planning/phases/01-data-foundation/01-UI-SPEC.md (Layout Structure, Copywriting Contract セクション)
    lib/types.ts (FilterParams, MarketDisplay型)
    lib/db.ts (prisma client importパス)
    prisma/schema.prisma (Marketモデル構造、インデックス)
  </read_first>
  <action>
1. lib/queries.ts を作成 — DBクエリをServer Componentから分離:
   ```typescript
   import { prisma } from '@/lib/db';
   import type { FilterParams, MarketDisplay } from '@/lib/types';
   import type { Prisma } from '@prisma/client';

   export async function getFilteredMarkets(
     params: FilterParams
   ): Promise<MarketDisplay[]> {
     const where: Prisma.MarketWhereInput = {
       isActive: true,
     };

     if (params.theme) {
       where.theme = params.theme;
     }

     if (params.minVolume) {
       where.volume24h = { gte: params.minVolume };
     }

     const orderBy: Prisma.MarketOrderByWithRelationInput =
       params.sortBy === 'change'
         ? { change24h: 'desc' }
         : params.sortBy === 'time'
         ? { updatedAt: 'desc' }
         : { volume24h: 'desc' }; // デフォルト: 出来高順

     const markets = await prisma.market.findMany({
       where,
       orderBy,
       take: 100,
       select: {
         id: true,
         titleJa: true,
         question: true,
         contextNote: true,
         theme: true,
         currentPrice: true,
         change24h: true,
         volume24h: true,
         updatedAt: true,
       },
     });

     return markets.map((m) => ({
       ...m,
       isSpike: Math.abs(m.change24h) >= 0.05, // 5%以上を急変表示
     }));
   }
   ```

2. app/(dashboard)/page.tsx を更新（Server Component）:
   - searchParamsからtheme, minVolume, sortByを取得
   - getFilteredMarkets()を呼び出し
   - MarketFilters + MarketTable コンポーネントをレンダリング
   - ページタイトル「予測市場一覧」をDisplay 28px/600で表示
   - Containerクラス: `container mx-auto px-4 py-16`（3xl spacing）

3. app/(dashboard)/loading.tsx を作成:
   - shadcn/ui Skeletonを使用
   - 10行分のスケルトンを表示
   - テキスト「市場データを読み込み中...」を表示（UI-SPECのCopywriting Contract準拠）

4. app/(dashboard)/error.tsx を作成（'use client'）:
   - 見出し「データの読み込みに失敗しました」
   - 本文「ネットワーク接続を確認して、再試行ボタンを押してください」
   - 「再試行」ボタンでreset()を呼び出し
   - カラー: bg-red-50, border-red-200, text-red-800（UI-SPEC準拠）
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/queries.ts contains "export async function getFilteredMarkets"
    - lib/queries.ts contains "prisma.market.findMany"
    - lib/queries.ts contains "isActive: true"
    - lib/queries.ts contains "volume24h: 'desc'"
    - lib/queries.ts contains "Math.abs(m.change24h) >= 0.05"
    - app/(dashboard)/page.tsx contains "getFilteredMarkets"
    - app/(dashboard)/page.tsx contains "searchParams"
    - app/(dashboard)/page.tsx contains "MarketTable"
    - app/(dashboard)/page.tsx contains "予測市場一覧"
    - app/(dashboard)/loading.tsx contains "Skeleton"
    - app/(dashboard)/loading.tsx contains "市場データを読み込み中"
    - app/(dashboard)/error.tsx contains "'use client'"
    - app/(dashboard)/error.tsx contains "データの読み込みに失敗しました"
    - app/(dashboard)/error.tsx contains "再試行"
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Server Componentがフィルタパラメータを受け取りDBクエリを実行し、Loading/Error状態が表示される</done>
</task>

<task type="auto">
  <name>Task 2: 市場テーブル + フィルタUI（Client Components）+ レスポンシブ対応</name>
  <files>
    components/market-table.tsx, components/market-filters.tsx,
    components/market-row.tsx, components/empty-state.tsx,
    tests/market-filters.test.ts
  </files>
  <read_first>
    .planning/phases/01-data-foundation/01-UI-SPEC.md (全セクション — Layout Structure, Interaction States, Color, Typography, Spacing Scale, Component Inventory, Responsive breakpoints)
    .planning/phases/01-data-foundation/01-CONTEXT.md (一覧表示セクション — 各行の表示項目)
    .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 1のClient Componentコード例, Pitfall 4: フィルタ状態管理, Pitfall 5: レスポンシブ対応)
    lib/types.ts (Theme, THEME_LABELS, FilterParams, MarketDisplay)
    components/ui/ (shadcn/uiコンポーネントのimportパスを確認)
  </read_first>
  <action>
1. components/market-filters.tsx を作成（'use client'）:
   - shadcn/ui Select, Input, Button を使用
   - フィルタ項目: テーマ選択（全テーマ + 8テーマ）、最小出来高（Input type=number）、ソート軸（出来高順/変動率順/更新順）
   - フィルタ変更時: URLSearchParamsを構築し、router.push()でナビゲーション
   - URL paramsをdefaultValuesとして読み込む（useSearchParams()）
   - デザイン仕様:
     - フィルタバー背景: #F9FAFB (bg-gray-50)
     - パディング: lg (24px = p-6)
     - フィールド間隔: md (16px = gap-4)
     - 「絞り込み」ボタン: bg-blue-600 text-white, hover:bg-blue-700
     - レスポンシブ: md以上で横並び(flex-row)、モバイルで縦並び(flex-col)

2. components/market-row.tsx を作成 — 1行分の表示:
   - Props: MarketDisplay
   - 表示項目（CONTEXT.md準拠）:
     a. 日本語タイトル（titleJa、nullなら「（翻訳中）」をtext-gray-400で表示）
     b. 文脈補足1行（contextNote、text-sm text-gray-500）
     c. テーマタグ（shadcn/ui Badge、bg-blue-100 text-blue-800）
     d. 現在確率（currentPrice * 100、小数点1桁、%表示）
     e. 24h変動（change24h * 100、小数点1桁、プラスは+記号 + text-green-600、マイナスはtext-red-600）
     f. 出来高（volume24h、$とtoLocaleString()でフォーマット）
     g. 急変バッジ（isSpike=trueの場合、Badge variant="destructive" で「急変」と表示、bg-amber-100 text-amber-800）
   - 行ホバー: bg-gray-50、transition-colors duration-150
   - テキスト: Body 14px (text-sm)

3. components/market-table.tsx を作成（'use client'）:
   - shadcn/ui Table, TableHeader, TableBody, TableRow, TableHead, TableCell を使用
   - ヘッダー列: 市場タイトル、文脈補足、テーマ、現在確率、24h変動、出来高、状態
   - ヘッダー背景: #F9FAFB (bg-gray-50)
   - テキスト: Label 12px/600 (text-xs font-semibold)
   - デスクトップ（>=1024px）: 全カラム表示
   - タブレット（768-1023px）: 文脈補足カラムを hidden md:table-cell
   - モバイル（<768px）: テーブルを非表示にし、代わりにカード形式の縦並びリストを表示
     - 各カードに: タイトル、テーマBadge、確率、変動、出来高を縦並び
     - カード間隔: sm (8px = space-y-2)
   - 空の場合: EmptyState コンポーネントを表示

4. components/empty-state.tsx を作成:
   - 見出し「該当する市場が見つかりませんでした」（text-xl font-semibold text-gray-500）
   - 本文「フィルタ条件を変更するか、出来高の最小値を下げてみてください」（text-sm text-gray-400）
   - 中央配置（text-center py-12）

5. tests/market-filters.test.ts を作成:
   - FilterParamsの型バリデーション
   - テーマ選択が全8テーマ+全テーマオプションを含むこと
   - ソート軸が3種類（volume, change, time）であること
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && npx vitest run tests/market-filters.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - components/market-filters.tsx contains "'use client'"
    - components/market-filters.tsx contains "router.push"
    - components/market-filters.tsx contains "useSearchParams"
    - components/market-filters.tsx contains "URLSearchParams"
    - components/market-filters.tsx contains "絞り込み"
    - components/market-filters.tsx contains "THEME_LABELS"
    - components/market-table.tsx contains "'use client'"
    - components/market-table.tsx contains "TableHeader"
    - components/market-table.tsx contains "MarketRow" or contains "market-row"
    - components/market-table.tsx contains "hidden" (responsive classes)
    - components/market-row.tsx contains "Badge"
    - components/market-row.tsx contains "text-green-600"
    - components/market-row.tsx contains "text-red-600"
    - components/market-row.tsx contains "（翻訳中）"
    - components/market-row.tsx contains "急変"
    - components/empty-state.tsx contains "該当する市場が見つかりませんでした"
    - components/empty-state.tsx contains "フィルタ条件を変更するか"
    - app/(dashboard)/page.tsx contains "MarketFilters"
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>市場一覧テーブルとフィルタUIが動作し、レスポンシブ対応（PC: テーブル、モバイル: カード）が完了</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: ダッシュボードUI目視確認</name>
  <files>N/A (verification only)</files>
  <what-built>
    市場一覧ダッシュボードUI全体:
    - テーブル表示（日本語タイトル、文脈補足、テーマタグ、確率、変動、出来高、急変バッジ）
    - フィルタUI（テーマ選択、最小出来高、ソート軸、絞り込みボタン）
    - レスポンシブ対応（PC: テーブル、モバイル: カード）
    - ローディング・エラー・空状態
  </what-built>
  <action>
    ユーザーにブラウザでの目視確認を依頼する。自動テストではカバーできないビジュアル・インタラクションの品質を検証する。
  </action>
  <how-to-verify>
    1. `npm run dev` でhttp://localhost:3000 を開く
    2. ページタイトル「予測市場一覧」が表示されることを確認
    3. フィルタバー（テーマ選択、出来高入力、ソート選択、絞り込みボタン）が表示されることを確認
    4. テーブルまたはカード表示が表示されることを確認（データがない場合は「該当する市場が見つかりませんでした」の空状態が表示）
    5. ブラウザ幅を768px未満に縮小し、カード形式に切り替わることを確認
    6. フィルタを操作し、URLパラメータが変更されることを確認（例: ?theme=crypto）
    7. ブラウザの戻るボタンでフィルタ状態が保持されることを確認

    注意: DBにデータがない段階では空状態表示が正常です。データ投入はPlan 02のCron実行後に確認できます。
  </how-to-verify>
  <verify>
    <automated>npm run dev -- --port 3000 &amp; sleep 3 &amp;&amp; curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 &amp;&amp; kill %1</automated>
  </verify>
  <done>ユーザーがダッシュボードUIを目視確認し、"approved" と回答</done>
  <resume-signal>"approved" と入力するか、修正点を記述してください</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` がエラーなしで完了
- `npx vitest run --reporter=verbose` が全テストパス
- localhost:3000 でダッシュボードが表示される
- フィルタ操作でURLが変わり、リロード後もフィルタが保持される
- モバイル幅でカード表示に切り替わる
</verification>

<success_criteria>
- 市場一覧がテーブル形式で表示される（各行に7つの情報項目）
- テーマ・出来高・変動率・ソート軸の4つのフィルタが動作する
- フィルタ状態がURL Search Paramsで管理され、リロード・ブラウザバックで保持される
- レスポンシブ: デスクトップでテーブル、モバイルでカード形式
- ローディング時にスケルトン、エラー時にリトライUI、空結果時に案内テキスト
- UI-SPECのカラー・タイポグラフィ・スペーシングに準拠
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-03-SUMMARY.md`
</output>
